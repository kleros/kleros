import { CollectedTriggerActions, TriggerActionForExecution } from "./types";
/**
 * Given a list of actions to perform, which *must be ordered
 * consistently across lambda runs*, collect them into a
 * `CollectedTriggerActions` structure. This structure is used
 * as an input to `ActionAPI` subclasses that implement the
 * `preflightCollectedTriggers` method.
 *
 * Downstream behavior depends on the fact that JavaScript
 * object properties with string keys are enumerated in
 * order of property creation. For more information, see:
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for...in
 *
 * @param {TriggerActionForExecution[]} actionsToPerform
 * @returns {CollectedTriggerActions}
 */
export declare function collectActionsByKindAndTriggerId(actionsToPerform: TriggerActionForExecution[]): CollectedTriggerActions;
