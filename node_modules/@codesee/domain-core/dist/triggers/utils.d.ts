import type { FileChange, Trigger } from "../endpointTypes/triggers";
import { GitHubFile } from "../githubTypes";
import type { TriggerRow } from "./types";
/**
 * Convert a row from the triggers table in the db to
 * a Trigger object suitable for sending over the wire,
 * and performing actions with.
 *
 * @param {TriggerRow} row - row from triggers table.
 * @param {string | null} currentUserId - id of user
 *   making request if there is one, null if not
 *   applicable, for example in a lambda.
 * @returns {Trigger}
 */
export declare function triggerFromDbRow(row: TriggerRow, currentUserId: string | null): Trigger;
export declare function githubFileToTriggerFileChange(file: GitHubFile): FileChange;
