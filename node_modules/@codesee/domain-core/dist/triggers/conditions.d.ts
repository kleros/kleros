import { FileChange, Trigger, TriggerCondition } from "../endpointTypes/triggers";
/**
 * Returns an array of Trigger ids that should run against a specific set of
 * file changes.
 */
export declare function getTriggersAffectedByFileChanges(fileChanges: FileChange[], triggers: Trigger[]): Trigger[];
export declare function filePathMatchesCondition(filePath: string, condition: TriggerCondition): boolean;
