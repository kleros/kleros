export declare type Params<Key extends string = string> = {
    readonly [key in Key]: string | undefined;
};
/**
 * Generates a URL-safe path from a string and a set of parameters
 *
 * @example generateEncodedPath("/app/:appId", { appId: "1" })
 * // "/app/1"
 */
export declare function generateEncodedPath(path: string, params?: Params): string;
